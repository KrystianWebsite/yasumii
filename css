header,
.hero-section {
  max-width: 1200px;
  margin: 0 auto;
}
.hero-section {
  max-width: none !important; /* lub usuń max-width całkowicie */
  width: 100%;
  margin: 0;
  border-radius: 0; /* jeśli nie chcesz zaokrąglonych rogów */
}
